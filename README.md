# gemini-canvas-md-copy

To install dependencies:

```bash
bun install
```

To build:

```bash
bun pack
```

# Added Features
- KaTeX / LaTeX support (math-inline, math-block) — `data-math` → `$...$` / `$$...$$`
